__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.env
.venv
venv/
data/*.db

# Vector store — schema is version-specific, never commit
vector_db/

# Streamlit secrets — never commit real keys (keep secrets.toml.example only)
.streamlit/secrets.toml

# macOS
.DS_Store
