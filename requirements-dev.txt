# Dev / full-power extras for TrustLLM — install ON TOP of requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Kept separate so the Streamlit Community Cloud deploy stays torch-free.
# (RAGAS faithfulness + context metrics already ship in requirements.txt; these
# add the torch-dependent extras + the test suite.)
# Every feature that uses these degrades gracefully when they are absent.

# Offline test suite + CI quality gate
pytest>=8.0.0

# Industry-standard GenAI metrics (evaluation_engine/deepeval_runner.py)
deepeval>=1.0.0

# RAG Debugger — BGE embedding option + RAGAS answer_relevancy (need torch)
langchain-huggingface>=0.1.0
sentence-transformers>=2.2.0   # pulls torch — only needed when the BGE option is selected
