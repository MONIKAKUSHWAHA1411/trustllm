streamlit>=1.39.0

langchain>=0.3.7
langchain-community>=0.3.5
langchain-text-splitters>=0.3.2

chromadb>=1.5.0
protobuf<4
pypdf>=4.3.1

groq>=0.9.0

plotly
reportlab

# Auth & env
python-dotenv>=1.0.1
authlib>=1.0.0

# Email
resend>=2.0.0

# Pro: cloud model providers (BYOK — bring your own key)
anthropic>=0.39.0
openai>=1.50.0
google-generativeai>=0.8.0
mistralai>=1.2.0
huggingface_hub>=0.26.0

# Pro: encrypt user API keys at rest
cryptography>=43.0.0

# ── Dev / full-power extras live in requirements-dev.txt ──────────────────
# Heavy or optional deps (pytest, deepeval, ragas, sentence-transformers/torch)
# are kept OUT of this file so the Streamlit Community Cloud deploy stays within
# its ~1 GB memory limit. The app degrades gracefully without them (RAG Debugger
# runs on fast embedding metrics; RAGAS/BGE show a "run locally" note).
# For the full local experience + test suite:
#     pip install -r requirements.txt -r requirements-dev.txt
