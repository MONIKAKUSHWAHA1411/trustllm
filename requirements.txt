streamlit>=1.39.0

langchain>=0.3.7
langchain-community>=0.3.5
langchain-text-splitters>=0.3.2

chromadb>=1.5.0
protobuf<4
pypdf>=4.3.1

groq>=0.9.0

# LangGraph Agent — real multi-step agent + trajectory eval (torch-free)
langgraph>=0.2.0

plotly
reportlab

# Auth & env
python-dotenv>=1.0.1
authlib>=1.0.0

# Email
resend>=2.0.0

# Pro: cloud model providers (BYOK — bring your own key)
anthropic>=0.39.0
openai>=1.50.0
google-generativeai>=0.8.0
mistralai>=1.2.0
huggingface_hub>=0.26.0

# Pro: encrypt user API keys at rest
cryptography>=43.0.0

# RAG Debugger — RAGAS scoring (torch-free). faithfulness + context_precision/
# recall use the Groq judge, no local embedding model, so this runs on Streamlit
# Community Cloud. answer_relevancy + BGE need torch → requirements-dev.txt;
# the app skips them gracefully when absent.
ragas>=0.2.0,<1.0
langchain-groq>=0.2.0
# ragas pulls HuggingFace `datasets`; pin both so the resolver can't pair an old
# datasets (calls pyarrow.PyExtensionType) with a newer pyarrow that removed it.
datasets>=2.20.0
pyarrow>=15.0.0

# ── Dev / full-power extras live in requirements-dev.txt ──────────────────
# Kept OUT of this file so the Streamlit Community Cloud deploy stays torch-free:
# the BGE embedding option, RAGAS answer_relevancy, DeepEval, and the test suite.
# The app degrades gracefully without them.
# For the full local experience + test suite:
#     pip install -r requirements.txt -r requirements-dev.txt
